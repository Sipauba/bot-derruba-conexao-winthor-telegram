UPDATE PCEMPR 
   SET USUARIOLOGADO = 'N',
       NUMCONEXOESATUAL = 0,
       DTINICIO = NULL,
       TIPOATENDE = NULL
WHERE MATRICULA = XXXXX

DELETE FROM PCMENUSESSAO 
      WHERE MATRICULA = XXXXXX

COMMIT